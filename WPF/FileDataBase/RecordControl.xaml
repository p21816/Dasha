<UserControl x:Class="FileDataBase.RecordControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <Label Content="Введите ID:"></Label>
        <TextBox Name="IDTextbox" Text="{Binding ElementName=recordControl, Path=ID}"></TextBox>
        <Label Content="Введите имя:"></Label>
        <TextBox Name="nameTextbox" Text="{Binding ElementName=recordControl, Path=Name}"></TextBox>
        <Label Content="Введите фамилию:"></Label>
        <TextBox Name="surTextbox" Text="{Binding ElementName=recordControl, Path=Surname}"></TextBox>
    </StackPanel>
</UserControl>
